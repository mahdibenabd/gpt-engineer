The application will start and be available at `http://127.0.0.1:5000` in your web browser.

## Running Tests

This project uses `pytest` for testing. To run the tests, execute the following command from the root directory: